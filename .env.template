# .env.template — placeholders only, never commit real values
# Copy this to .env and fill in actual values for local use.
# Real .env is listed in .gitignore and must never be committed.

APP_VERSION=your-image-version-here
APP_PORT=8080
